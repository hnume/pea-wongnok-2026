"use client";

const CartSummary = () => {
  // TODO: read from store
  const count = 0;

  return (
    <span className="rounded-lg border border-border px-3 py-1 wongnok-text-sm">
      🛒 {count}
    </span>
  );
};

export default CartSummary;
